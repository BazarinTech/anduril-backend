<?php
/**
 * Renamed to platform-control.php -- the page governs the claim window as well
 * as transaction settings now, so "Transaction Control" undersold it.
 *
 * This stub stays because the old path is in browser history, in bookmarks,
 * and possibly in a link somewhere neither of us has looked. A 301 costs
 * nothing and means none of those turn into a 404.
 *
 * Safe to delete once nothing points here.
 */

header('Location: platform-control', true, 301);
exit;
